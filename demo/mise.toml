#:schema https://mise.jdx.dev/schema/mise.json

[tasks]
  [tasks.dev]
  wait_for = [ "//:sync" ]
  run = "guildkit dev"

  [tasks.build]
  wait_for = [ "//:sync" ]
  run = "guildkit build"
  env = { NODE_ENV = "production" }

  [tasks.lint]
  wait_for = [ "//:sync" ]
  run = "tsc --noEmit"

  [tasks.deploy]
  depends = [ "build" ]
  run = "wrangler deploy"

[env]
_.path = "./node_modules/.bin"
