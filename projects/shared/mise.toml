#:schema https://mise.jdx.dev/schema/mise.json

[tasks]
  [tasks.dev]
  run = "tsdown --watch"

  [tasks.build]
  run = [
    { tasks = [ "build:*" ] },
    "tsdown",
  ]
  env = { NODE_ENV = "production" }

  [tasks.lint]
  run = "tsc --noEmit"

[env]
_.path = "./node_modules/.bin"
