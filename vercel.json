{
  "$schema": "https://openapi.vercel.sh/vercel.json",

  "framework": "nextjs",

  "installCommand": "dnf install -y 'dnf-command(config-manager)' && dnf config-manager --add-repo https://mise.jdx.dev/rpm/mise.repo && dnf install -y mise && mise install",
  "buildCommand": "mise build",
  "devCommand": "mise dev"
}
